# HyperSCA 项目术语与表达指南

## 基本写法

正文采用“通俗名称（标准术语）”。先回答这一步研究什么、为什么需要、结果能说明到什么程度。标准术语供检索和专业沟通使用，后文在不产生歧义时可只用通俗名称。

用户可见文字通常按“用途—做法—限制”的顺序组织。一个句子只承担一个主要信息，避免用多个缩写、斜杠或括号串联研究步骤。

## 适用范围

本指南适用于 README、当前维护的研究文档、命令行帮助、错误提示、运行摘要和图注。历史报告、发布记录、文献标题、引文及 Bear 检索原始记录保留原貌。

## 兼容性边界

不重命名 Python 名称、命令参数、JSON/CSV 字段、文件或目录。文献标题、引文和第三方软件正式名称保持原文。代码、命令、字段名和路径使用反引号，并在附近解释研究含义。

## 科学边界

不得把相关改写成因果，不得把预测或模拟写成实验验证，不得把候选写成有效治疗，也不得把补充证据写成决定性证据。

- 相关、预测、富集或评分结果只能说明观察到的模式或模型给出的估计。
- 合成数据上的最小运行检查不能代替外部真实数据验证。
- 候选靶点进入下一证据阶段，不等于已经证明药物机制或临床疗效。
- 独立的补充分析（sidecar）用于提供旁证，不改变预先规定的主要结论或候选排序。

## 首选术语

| 标准术语或现有名称 | 用户可见的首选表达 | 必须补充的含义 |
|---|---|---|
| pipeline | 分析流程 | 数据依次经过哪些研究步骤 |
| workflow | 工作流程 | 谁在什么顺序执行哪些步骤 |
| benchmark | 对照评估 | 在相同条件下比较方法 |
| benchmark contract | 预先固定的比较规则 | 提前确定数据、指标和通过条件 |
| baseline | 简单对照方法 | 用来判断复杂方法是否真的带来增益 |
| holdout | 未参与模型建立的独立验证数据 | 不用于选特征或调参数 |
| artifact | 分析输出文件 | 可复查的表格、图或记录文件 |
| manifest | 分析记录清单 | 记录输入、版本、参数和输出位置 |
| sidecar | 独立的补充分析 | 提供旁证，但不改变主结果 |
| adapter | 数据或方法接入层 | 把外部格式转成项目可读取的形式 |
| promotion gate | 进入下一证据阶段的条件 | 达标才允许提高结论等级 |
| evidence-gated ranking | 由直接证据决定的排序 | 补充模型不能越过直接证据改名次 |
| coverage | 可作出判断的比例 | 有足够信息并给出结果的对象占比 |
| abstention | 暂不判断 | 信息不足时明确不作预测或结论 |
| null control | 零效应对照或负控 | 检查随机关系是否也会产生类似结果 |
| bootstrap | 重复抽样检查 | 通过多次抽样评估结果稳定性 |
| split | 数据划分 | 哪些数据用于建立、调整和验证方法 |
| tuning budget | 参数尝试上限 | 各方法允许尝试的设置次数相同 |
| smoke test | 最小运行检查 | 只证明流程能运行，不证明科学有效 |
| fail closed | 条件不全时停止并判为未通过 | 不用默认值掩盖缺失证据 |
| CLI | 命令行使用方式 | 通过终端运行脚本的方法 |
| schema | 数据字段规范 | 必须包含哪些字段及其含义 |
| digest / hash | 文件内容指纹 | 用于确认输入文件没有被悄悄替换 |

该表是首选表达清单，不是机械替换表。改写时必须结合上下文，避免错误替换第三方正式名称、统计定义或稳定接口。

## 命令帮助和错误提示

命令参数名保持不变。参数说明应交代研究含义、预期单位、默认行为，以及它是否影响主要结论。

错误提示采用“发生了什么—为什么不能继续—用户该做什么”的顺序。例如：

> 独立验证数据缺少 `spatial_block` 字段，因此无法确认训练区域与验证区域是否分开。请按数据字段规范补充该列后重新运行。

## 示例

推荐：因果稳定性检查会重复抽样，观察同一条关系是否反复出现。结果作为独立的补充分析（sidecar）保存，不改变候选靶点排序。

不推荐：Causal sidecar 生成 bootstrap artifact，不进入 ranking pipeline。

推荐：该最小运行检查（smoke test）只证明分析流程可以运行，不代表方法已经得到真实数据验证。

不推荐：Smoke test passed，因此模型有效。
